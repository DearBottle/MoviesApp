<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--Appbar 主题-->
    <style name="DxAppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <!--toolbar 弹出菜单主题-->
    <style name="DxPopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="actionOverflowMenuStyle">@style/OverflowMenuStyle</item>
    </style>


    <style name="OverflowMenuStyle" parent="Widget.AppCompat.Light.PopupMenu.Overflow">
        <!--把该属性改为false即可使menu位置位于toolbar之下-->
        <item name="overlapAnchor">false</item>
    </style>

    <!--背景透明对话框-->
    <style name="DxTransparentDialog" parent="ThemeOverlay.AppCompat.Dialog.Alert">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>

    </style>

    <!--背景dim对话框-->
    <style name="DxTransparentDialog.Dim">
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>